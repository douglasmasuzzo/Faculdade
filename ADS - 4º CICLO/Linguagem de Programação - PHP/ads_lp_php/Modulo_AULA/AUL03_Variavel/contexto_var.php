<?php
    /* 
     # DEFINIÇÃO - VARIÁVEIS
        espaço atribuído à memória ram que armazena informações temporáriamente, de maneira mutável e imutável, durante a execução de um programa.

     # SINTAXE
        são representadas por título, tipo de variável e o valor armazenado, juntamente com o caractere de exibição '$'
    */

    $varialvel;
     // variável alocada na memória, sem inicialização  

    $var = "teste";
     // variável atribuída com um tipo de valor textual ( String )
    
    $var_iavel = 100;
     // variável atribuída a um valor assumindo outro valor
    
    echo $var; echo $var_iavel;
     // exibição do conteúdo atribuído a variável

    # FORMAS INADEQUADAS
    /*
        $1VAR = 0;
        $!var = 0;
        $variavel( 1 );
        $variavel um = 0;
    */

    #FORMAS ADEQUADAS
     $var1 = 0;
     $var_1 = 0;
     $_1 = 0;

    /*
     # NOMENCLATURA DE VARIÁVEIS
        estilos de nomenclatura : camelCase, snake_Case, "studyCase" , PascalCase
    */ 


?>  